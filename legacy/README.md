# Legacy static UI

Pre–Next.js dashboard (`index.html` + `app.js`). Superseded by `frontend/` (static export served from `frontend/out/` via FastAPI).

To open locally: serve `static-ui/` with any static file server and point API requests to `:8000`.

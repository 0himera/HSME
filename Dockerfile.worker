FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml uv.lock ./
RUN pip install uv && uv sync --frozen --no-dev

COPY backend ./backend

ENV PYTHONPATH=/app

CMD ["uv", "run", "python", "-m", "backend.workers.neo4j_consumer"]
